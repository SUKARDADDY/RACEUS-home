# RACEUS-home

The library page at raceus.co.il: every project and service under the domain, one card each.
Static, no build step. Deployed to Cloudflare Pages by GitHub Actions on push to `main`.

Scaffold commit. The site lands in the first pull request.
